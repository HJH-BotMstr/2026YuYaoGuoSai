flowchart TB
    subgraph 感知层
        CAM["单目相机<br/>/camera/raw"]
        DEP["深度相机<br/>/depth/..."]
        ULT["超声波<br/>/ultrasonic"]
    end

    subgraph 视觉与感知节点
        GD["gauge_detector<br/>(C++ / Python)<br/>输出：/gauge/status"]
        LD["letter_detector<br/>(新增)<br/>输出：/letter/detection"]
    end

    subgraph 决策层
        MC["mission_control_node<br/>- 状态机管理<br/>- 避障闭环<br/>- 视觉伺服对齐<br/>- 语音播报触发"]
    end

    subgraph 控制指令层
        VEL["/cmd_vel<br/>(Twist)"]
        GAIT["/cmd_gait<br/>(String)"]
        VOICE["/voice/play<br/>(String)"]
    end

    subgraph 执行层
        DRV_LEG["lite3_ros2_<br/>driver_legodom<br/>(UDP 运动主机)"]
        DRV["lite3_ros2_<br/>driver"]
        VBN["voice_broadcast_<br/>node<br/>(USB 免驱扬声器)"]
    end

    CAM --> GD
    CAM --> LD
    DEP --> MC
    ULT --> MC
    GD --> MC
    LD --> MC
    MC --> VEL
    MC --> GAIT
    MC --> VOICE
    VEL --> DRV_LEG
    GAIT --> DRV
    VOICE --> VBN